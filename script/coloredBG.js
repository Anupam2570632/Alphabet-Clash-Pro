function coloredBg(char) {
    const textItem = document.getElementById(char);
    textItem.style.backgroundColor ='yellow';
}

function removeBg(char) {
    const textItem = document.getElementById(char);
    textItem.style.backgroundColor = 'white';
}